const { blog, editor } = require('./blog');
const { auth } = require('./auth');
const { home } = require('./home');
const { page } = require('./page');
const { site } = require('./site');


// exports
module.exports = { auth, blog, editor, home, page, site };
